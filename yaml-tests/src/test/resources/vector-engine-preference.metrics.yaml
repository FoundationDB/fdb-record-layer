vector-engine-preference-default-planner:
-   query: EXPLAIN select docId, euclidean_distance(embedding, ?) as distance from
        documents where zone = 'zone1' and bookshelf = 'fiction' qualify row_number()
        over ( partition by zone, bookshelf order by euclidean_distance(embedding,
        ?) asc ) <= 3
    ref: vector-engine-preference.yamsql:113
    explain: 'ISCAN(DOCUMENTSGUARDIANNINDEX [EQUALS promote(@c16 AS STRING), EQUALS
        promote(@c20 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c7: @c44}:[] BY_DISTANCE)
        | MAP (_.DOCID AS DOCID, (_.EMBEDDING) euclidean_distance @c7 AS DISTANCE)'
    task_count: 307
    task_total_time_ms: 30
    transform_count: 91
    transform_time_ms: 15
    transform_yield_count: 39
    insert_time_ms: 0
    insert_new_count: 27
    insert_reused_count: 2
-   query: EXPLAIN select docId as id from documents where zone = 'zone1' and bookshelf
        = 'fiction' qualify row_number() over ( partition by zone, bookshelf order
        by euclidean_distance(embedding, ?) asc ) <= 2 union all select articleId
        as id from articles where zone = 'zone1' and section = 'news' qualify row_number()
        over ( partition by zone, section order by euclidean_distance(embedding, ?)
        asc ) <= 2
    ref: vector-engine-preference.yamsql:131
    explain: 'ISCAN(DOCUMENTSGUARDIANNINDEX [EQUALS promote(@c9 AS STRING), EQUALS
        promote(@c13 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[]
        BY_DISTANCE) | MAP (_.DOCID AS ID) ⊎ ISCAN(ARTICLESHNSWINDEX [EQUALS promote(@c9
        AS STRING), EQUALS promote(@c53 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL
        @c31: @c37}:[] BY_DISTANCE) | MAP (_.ARTICLEID AS ID)'
    task_count: 610
    task_total_time_ms: 51
    transform_count: 185
    transform_time_ms: 24
    transform_yield_count: 79
    insert_time_ms: 0
    insert_new_count: 57
    insert_reused_count: 3
vector-engine-preference-prefer-hnsw:
-   query: EXPLAIN select docId, euclidean_distance(embedding, ?) as distance from
        documents where zone = 'zone1' and bookshelf = 'fiction' qualify row_number()
        over ( partition by zone, bookshelf order by euclidean_distance(embedding,
        ?) asc ) <= 3
    ref: vector-engine-preference.yamsql:152
    explain: 'ISCAN(DOCUMENTSHNSWINDEX [EQUALS promote(@c16 AS STRING), EQUALS promote(@c20
        AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c7: @c44}:[] BY_DISTANCE) |
        MAP (_.DOCID AS DOCID, (_.EMBEDDING) euclidean_distance @c7 AS DISTANCE)'
    task_count: 307
    task_total_time_ms: 26
    transform_count: 91
    transform_time_ms: 14
    transform_yield_count: 39
    insert_time_ms: 0
    insert_new_count: 27
    insert_reused_count: 2
-   query: EXPLAIN select docId as id from documents where zone = 'zone1' and bookshelf
        = 'fiction' qualify row_number() over ( partition by zone, bookshelf order
        by euclidean_distance(embedding, ?) asc ) <= 2 union all select articleId
        as id from articles where zone = 'zone1' and section = 'news' qualify row_number()
        over ( partition by zone, section order by euclidean_distance(embedding, ?)
        asc ) <= 2
    ref: vector-engine-preference.yamsql:170
    explain: 'ISCAN(DOCUMENTSHNSWINDEX [EQUALS promote(@c9 AS STRING), EQUALS promote(@c13
        AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[] BY_DISTANCE)
        | MAP (_.DOCID AS ID) ⊎ ISCAN(ARTICLESHNSWINDEX [EQUALS promote(@c9 AS STRING),
        EQUALS promote(@c53 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[]
        BY_DISTANCE) | MAP (_.ARTICLEID AS ID)'
    task_count: 610
    task_total_time_ms: 28
    transform_count: 185
    transform_time_ms: 13
    transform_yield_count: 79
    insert_time_ms: 0
    insert_new_count: 57
    insert_reused_count: 3
-   query: EXPLAIN select docId as id from guardiannOnly where zone = 'zone1' and
        bookshelf = 'fiction' qualify row_number() over ( partition by zone, bookshelf
        order by euclidean_distance(embedding, ?) asc ) <= 2
    ref: vector-engine-preference.yamsql:180
    explain: 'ISCAN(GUARDIANNONLYINDEX [EQUALS promote(@c9 AS STRING), EQUALS promote(@c13
        AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[] BY_DISTANCE)
        | MAP (_.DOCID AS ID)'
    task_count: 199
    task_total_time_ms: 17
    transform_count: 62
    transform_time_ms: 8
    transform_yield_count: 22
    insert_time_ms: 0
    insert_new_count: 17
    insert_reused_count: 1
-   query: EXPLAIN select docId as id from mixedMetrics where zone = 'zone1' and bookshelf
        = 'fiction' qualify row_number() over ( partition by zone, bookshelf order
        by cosine_distance(embedding, ?) asc ) <= 2
    ref: vector-engine-preference.yamsql:191
    explain: 'ISCAN(MIXEDMETRICSGUARDIANNCOSINEINDEX [EQUALS promote(@c9 AS STRING),
        EQUALS promote(@c13 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[]
        BY_DISTANCE) | MAP (_.DOCID AS ID)'
    task_count: 211
    task_total_time_ms: 18
    transform_count: 66
    transform_time_ms: 9
    transform_yield_count: 28
    insert_time_ms: 0
    insert_new_count: 17
    insert_reused_count: 1
vector-engine-preference-prefer-guardiann:
-   query: EXPLAIN select docId, euclidean_distance(embedding, ?) as distance from
        documents where zone = 'zone1' and bookshelf = 'fiction' qualify row_number()
        over ( partition by zone, bookshelf order by euclidean_distance(embedding,
        ?) asc ) <= 3
    ref: vector-engine-preference.yamsql:212
    explain: 'ISCAN(DOCUMENTSGUARDIANNINDEX [EQUALS promote(@c16 AS STRING), EQUALS
        promote(@c20 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c7: @c44}:[] BY_DISTANCE)
        | MAP (_.DOCID AS DOCID, (_.EMBEDDING) euclidean_distance @c7 AS DISTANCE)'
    task_count: 307
    task_total_time_ms: 27
    transform_count: 91
    transform_time_ms: 14
    transform_yield_count: 39
    insert_time_ms: 0
    insert_new_count: 27
    insert_reused_count: 2
-   query: EXPLAIN select docId as id from documents where zone = 'zone1' and bookshelf
        = 'fiction' qualify row_number() over ( partition by zone, bookshelf order
        by euclidean_distance(embedding, ?) asc ) <= 2 union all select articleId
        as id from articles where zone = 'zone1' and section = 'news' qualify row_number()
        over ( partition by zone, section order by euclidean_distance(embedding, ?)
        asc ) <= 2
    ref: vector-engine-preference.yamsql:230
    explain: 'ISCAN(DOCUMENTSGUARDIANNINDEX [EQUALS promote(@c9 AS STRING), EQUALS
        promote(@c13 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[]
        BY_DISTANCE) | MAP (_.DOCID AS ID) ⊎ ISCAN(ARTICLESGUARDIANNINDEX [EQUALS
        promote(@c9 AS STRING), EQUALS promote(@c53 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL
        @c31: @c37}:[] BY_DISTANCE) | MAP (_.ARTICLEID AS ID)'
    task_count: 610
    task_total_time_ms: 19
    transform_count: 185
    transform_time_ms: 9
    transform_yield_count: 79
    insert_time_ms: 0
    insert_new_count: 57
    insert_reused_count: 3
-   query: EXPLAIN select docId as id from hnswOnly where zone = 'zone1' and bookshelf
        = 'fiction' qualify row_number() over ( partition by zone, bookshelf order
        by euclidean_distance(embedding, ?) asc ) <= 2
    ref: vector-engine-preference.yamsql:240
    explain: 'ISCAN(HNSWONLYINDEX [EQUALS promote(@c9 AS STRING), EQUALS promote(@c13
        AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[] BY_DISTANCE)
        | MAP (_.DOCID AS ID)'
    task_count: 199
    task_total_time_ms: 15
    transform_count: 62
    transform_time_ms: 7
    transform_yield_count: 22
    insert_time_ms: 0
    insert_new_count: 17
    insert_reused_count: 1
-   query: EXPLAIN select docId as id from mixedMetrics where zone = 'zone1' and bookshelf
        = 'fiction' qualify row_number() over ( partition by zone, bookshelf order
        by euclidean_distance(embedding, ?) asc ) <= 2
    ref: vector-engine-preference.yamsql:250
    explain: 'ISCAN(MIXEDMETRICSHNSWEUCLIDEANINDEX [EQUALS promote(@c9 AS STRING),
        EQUALS promote(@c13 AS STRING)]:{DISTANCE_RANK_LESS_THAN_OR_EQUAL @c31: @c37}:[]
        BY_DISTANCE) | MAP (_.DOCID AS ID)'
    task_count: 211
    task_total_time_ms: 19
    transform_count: 66
    transform_time_ms: 9
    transform_yield_count: 28
    insert_time_ms: 0
    insert_new_count: 17
    insert_reused_count: 1
-   query: EXPLAIN select docId from documents where title = 'The Great Gatsby'
    ref: vector-engine-preference.yamsql:255
    explain: 'COVERING(DOCUMENTSBYTITLE [EQUALS promote(@c7 AS STRING)] -> [DOCID:
        KEY:[3], TITLE: KEY:[0], ZONE: KEY:[2]]) | MAP (_.DOCID AS DOCID)'
    task_count: 475
    task_total_time_ms: 29
    transform_count: 120
    transform_time_ms: 9
    transform_yield_count: 48
    insert_time_ms: 0
    insert_new_count: 49
    insert_reused_count: 4
