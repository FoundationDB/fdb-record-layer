recursive-cte-tests:
-   query: EXPLAIN with recursive c1 as ( select id, parent, 0 as level from t1 where
        id = 250 union all select b.id, b.parent, a.y as level from t1 as b, (select
        id, parent, level + 1 as y from c1) as a  where a.parent = b.id) traversal
        order pre_order select id, parent, level from c1
    ref: recursive-cte.yamsql:41
    explain: 'RUNION-DFS PREORDER q0 { SCAN([IS T1, EQUALS promote(@c18 AS LONG)])
        | MAP (_.ID AS ID, _.PARENT AS PARENT, @c10 AS LEVEL) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.PARENT EQUALS _.ID | MAP
        (_.ID AS ID, _.PARENT AS PARENT, q0.LEVEL + @c48 AS LEVEL) } | MAP (_.ID AS
        ID, _.PARENT AS PARENT, _.LEVEL AS LEVEL)'
    task_count: 1849
    task_total_time_ms: 180
    transform_count: 413
    transform_time_ms: 38
    transform_yield_count: 120
    insert_time_ms: 4
    insert_new_count: 255
    insert_reused_count: 10
-   query: EXPLAIN with recursive c1 as ( select id, parent, 0 as level from t1 where
        id = 250 union all select b.id, b.parent, a.y as level from t1 as b, (select
        id, parent, level + 1 as y from c1) as a  where a.parent = b.id) traversal
        order post_order select id, parent, level from c1
    ref: recursive-cte.yamsql:54
    explain: 'RUNION-DFS POSTORDER q0 { SCAN([IS T1, EQUALS promote(@c18 AS LONG)])
        | MAP (_.ID AS ID, _.PARENT AS PARENT, @c10 AS LEVEL) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.PARENT EQUALS _.ID | MAP
        (_.ID AS ID, _.PARENT AS PARENT, q0.LEVEL + @c48 AS LEVEL) } | MAP (_.ID AS
        ID, _.PARENT AS PARENT, _.LEVEL AS LEVEL)'
    task_count: 1849
    task_total_time_ms: 80
    transform_count: 413
    transform_time_ms: 24
    transform_yield_count: 120
    insert_time_ms: 3
    insert_new_count: 255
    insert_reused_count: 10
-   query: EXPLAIN with recursive c1 as ( select id, parent, 0 as level from t1 where
        id = 250 union all select b.id, b.parent, a.y as level from (select id, parent,
        level + 1 as y from c1) as a, t1 as b where a.parent = b.id) traversal order
        level_order select id, parent, level from c1
    ref: recursive-cte.yamsql:67
    explain: RUNION q0, q1 { INITIAL { SCAN([IS T1, EQUALS promote(@c18 AS LONG)])
        | MAP (_.ID AS ID, _.PARENT AS PARENT, @c10 AS LEVEL) | INSERT INTO TEMP q1
        } RECURSIVE  { ISCAN(CHILDIDX <,>) | FLATMAP q2 -> { TEMP SCAN base() | FILTER
        _.PARENT EQUALS q2.ID AS q3 RETURN (q2.ID AS ID, q2.PARENT AS PARENT, q3.LEVEL
        + @c44 AS LEVEL) } | INSERT INTO TEMP q1 }} | MAP (_.ID AS ID, _.PARENT AS
        PARENT, _.LEVEL AS LEVEL)
    task_count: 1442
    task_total_time_ms: 105
    transform_count: 357
    transform_time_ms: 28
    transform_yield_count: 105
    insert_time_ms: 3
    insert_new_count: 195
    insert_reused_count: 10
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where parent =
        -1 union all select b.id, b.parent from c1 as a, t1 as b where a.id = b.parent)
        select id from c1
    ref: recursive-cte.yamsql:77
    explain: 'RUNION-DFS PREORDER q0 { ISCAN(PARENTIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.ID EQUALS _.PARENT | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 1845
    task_total_time_ms: 108
    transform_count: 394
    transform_time_ms: 28
    transform_yield_count: 124
    insert_time_ms: 3
    insert_new_count: 260
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where parent =
        -1 union all select b.id, b.parent from c1 as a, t1 as b where a.id = b.parent)
        traversal order post_order select id from c1
    ref: recursive-cte.yamsql:107
    explain: 'RUNION-DFS POSTORDER q0 { ISCAN(PARENTIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.ID EQUALS _.PARENT | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 1806
    task_total_time_ms: 175
    transform_count: 388
    transform_time_ms: 38
    transform_yield_count: 121
    insert_time_ms: 4
    insert_new_count: 252
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where parent =
        -1 union all select b.id, b.parent from c1 as a, t1 as b where a.id = b.parent)
        traversal order level_order select id from c1
    ref: recursive-cte.yamsql:125
    explain: RUNION q0, q1 { INITIAL { ISCAN(PARENTIDX [EQUALS promote(@c14 AS LONG)])
        | INSERT INTO TEMP q1 } RECURSIVE  { ISCAN(CHILDIDX <,>) | FLATMAP q2 -> {
        TEMP SCAN base() | FILTER _.ID EQUALS q2.PARENT AS q3 RETURN (q2.ID AS ID,
        q2.PARENT AS PARENT) } | INSERT INTO TEMP q1 }} | MAP (_.ID AS ID)
    task_count: 1399
    task_total_time_ms: 81
    transform_count: 332
    transform_time_ms: 22
    transform_yield_count: 106
    insert_time_ms: 2
    insert_new_count: 192
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where id = 250
        union all select b.id, b.parent from c1 as a, t1 as b where a.parent = b.id)
        select id from c1
    ref: recursive-cte.yamsql:140
    explain: 'RUNION-DFS PREORDER q0 { ISCAN(CHILDIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 2191
    task_total_time_ms: 107
    transform_count: 436
    transform_time_ms: 29
    transform_yield_count: 135
    insert_time_ms: 3
    insert_new_count: 315
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where id = 250
        union all select b.id, b.parent from c1 as a, t1 as b where a.parent = b.id)
        traversal order level_order select id from c1
    ref: recursive-cte.yamsql:153
    explain: RUNION q0, q1 { INITIAL { ISCAN(CHILDIDX [EQUALS promote(@c14 AS LONG)])
        | INSERT INTO TEMP q1 } RECURSIVE  { ISCAN(CHILDIDX <,>) | FLATMAP q2 -> {
        TEMP SCAN base() | FILTER _.PARENT EQUALS q2.ID AS q3 RETURN (q2.ID AS ID,
        q2.PARENT AS PARENT) } | INSERT INTO TEMP q1 }} | MAP (_.ID AS ID)
    task_count: 1319
    task_total_time_ms: 147
    transform_count: 316
    transform_time_ms: 31
    transform_yield_count: 101
    insert_time_ms: 2
    insert_new_count: 181
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where id = 250
        union all select b.id, b.parent from c1 as a, t1 as b where a.parent = b.id)
        traversal order pre_order select id from c1
    ref: recursive-cte.yamsql:166
    explain: 'RUNION-DFS PREORDER q0 { ISCAN(CHILDIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 2152
    task_total_time_ms: 170
    transform_count: 430
    transform_time_ms: 27
    transform_yield_count: 132
    insert_time_ms: 4
    insert_new_count: 305
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where id = 250
        union all select b.id, b.parent from c1 as a, t1 as b where a.parent = b.id)
        traversal order post_order select id from c1
    ref: recursive-cte.yamsql:179
    explain: 'RUNION-DFS POSTORDER q0 { ISCAN(CHILDIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 2152
    task_total_time_ms: 152
    transform_count: 430
    transform_time_ms: 26
    transform_yield_count: 132
    insert_time_ms: 4
    insert_new_count: 305
    insert_reused_count: 9
-   query: EXPLAIN with recursive allDescendants as ( with recursive ancestorsOf250
        as ( select id, parent from t1 where id = 250 union all select b.id, b.parent
        from ancestorsOf250 as a, t1 as b where a.parent = b.id) select id, parent
        from ancestorsOf250 union all select b.id, b.parent from allDescendants as
        a, t1 as b where a.id = b.parent) select id, parent from allDescendants
    ref: recursive-cte.yamsql:192
    explain: 'RUNION-DFS PREORDER q0 { RUNION-DFS PREORDER q1 { ISCAN(CHILDIDX [EQUALS
        promote(@c19 AS LONG)]) } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1],
        PARENT: KEY:[0]]) | FILTER q1.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT
        AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.ID EQUALS _.PARENT | MAP
        (_.ID AS ID, _.PARENT AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT)'
    task_count: 3425
    task_total_time_ms: 313
    transform_count: 700
    transform_time_ms: 68
    transform_yield_count: 207
    insert_time_ms: 8
    insert_new_count: 499
    insert_reused_count: 15
-   query: EXPLAIN with recursive allDescendants as ( with recursive ancestorsOf250
        as ( select id, parent from t1 where id = 250 union all select b.id, b.parent
        from ancestorsOf250 as a, t1 as b where a.parent = b.id) traversal order post_order
        select id, parent from ancestorsOf250 union all select b.id, b.parent from
        allDescendants as a, t1 as b where a.id = b.parent) traversal order post_order
        select id, parent from allDescendants
    ref: recursive-cte.yamsql:242
    explain: 'RUNION-DFS POSTORDER q0 { RUNION-DFS POSTORDER q1 { ISCAN(CHILDIDX [EQUALS
        promote(@c19 AS LONG)]) } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1],
        PARENT: KEY:[0]]) | FILTER q1.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT
        AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.ID EQUALS _.PARENT | MAP
        (_.ID AS ID, _.PARENT AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT)'
    task_count: 3135
    task_total_time_ms: 198
    transform_count: 659
    transform_time_ms: 52
    transform_yield_count: 193
    insert_time_ms: 6
    insert_new_count: 447
    insert_reused_count: 15
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where parent =
        -1 union all select b.id, b.parent from c1 as a, t1 as b where a.id = b.parent)
        select id from c1
    ref: recursive-cte.yamsql:266
    explain: 'RUNION-DFS PREORDER q0 { ISCAN(PARENTIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.ID EQUALS _.PARENT | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 1845
    task_total_time_ms: 108
    transform_count: 394
    transform_time_ms: 28
    transform_yield_count: 124
    insert_time_ms: 3
    insert_new_count: 260
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where parent =
        -1 union all select b.id, b.parent from c1 as a, t1 as b where a.id = b.parent)
        select id from c1
    ref: recursive-cte.yamsql:283
    explain: 'RUNION-DFS PREORDER q0 { ISCAN(PARENTIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.ID EQUALS _.PARENT | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 1845
    task_total_time_ms: 108
    transform_count: 394
    transform_time_ms: 28
    transform_yield_count: 124
    insert_time_ms: 3
    insert_new_count: 260
    insert_reused_count: 9
-   query: EXPLAIN with recursive c1 as ( select id, parent from t1 where parent =
        -1 union all select b.id, b.parent from c1 as a, t1 as b where a.id = b.parent)
        traversal order post_order select id from c1
    ref: recursive-cte.yamsql:308
    explain: 'RUNION-DFS POSTORDER q0 { ISCAN(PARENTIDX [EQUALS promote(@c14 AS LONG)])
        } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) |
        FILTER q0.ID EQUALS _.PARENT | MAP (_.ID AS ID, _.PARENT AS PARENT) } | MAP
        (_.ID AS ID)'
    task_count: 1806
    task_total_time_ms: 175
    transform_count: 388
    transform_time_ms: 38
    transform_yield_count: 121
    insert_time_ms: 4
    insert_new_count: 252
    insert_reused_count: 9
-   query: EXPLAIN with recursive allDescendants as ( with recursive ancestorsOf250
        as ( select id, parent from t1 where id = 250 union all select b.id, b.parent
        from ancestorsOf250 as a, t1 as b where a.parent = b.id) select id, parent
        from ancestorsOf250 union all select b.id, b.parent from allDescendants as
        a, t1 as b where a.id = b.parent) select id, parent from allDescendants
    ref: recursive-cte.yamsql:329
    explain: 'RUNION-DFS PREORDER q0 { RUNION-DFS PREORDER q1 { ISCAN(CHILDIDX [EQUALS
        promote(@c19 AS LONG)]) } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1],
        PARENT: KEY:[0]]) | FILTER q1.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT
        AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.ID EQUALS _.PARENT | MAP
        (_.ID AS ID, _.PARENT AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT)'
    task_count: 3425
    task_total_time_ms: 313
    transform_count: 700
    transform_time_ms: 68
    transform_yield_count: 207
    insert_time_ms: 8
    insert_new_count: 499
    insert_reused_count: 15
-   query: EXPLAIN with recursive allDescendants as ( with recursive ancestorsOf250
        as ( select id, parent from t1 where id = 250 union all select b.id, b.parent
        from ancestorsOf250 as a, t1 as b where a.parent = b.id) select id, parent
        from ancestorsOf250 union all select b.id, b.parent from allDescendants as
        a, t1 as b where a.id = b.parent) select id, parent from allDescendants
    ref: recursive-cte.yamsql:357
    explain: 'RUNION-DFS PREORDER q0 { RUNION-DFS PREORDER q1 { ISCAN(CHILDIDX [EQUALS
        promote(@c19 AS LONG)]) } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1],
        PARENT: KEY:[0]]) | FILTER q1.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT
        AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.ID EQUALS _.PARENT | MAP
        (_.ID AS ID, _.PARENT AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT)'
    task_count: 3425
    task_total_time_ms: 313
    transform_count: 700
    transform_time_ms: 68
    transform_yield_count: 207
    insert_time_ms: 8
    insert_new_count: 499
    insert_reused_count: 15
-   query: EXPLAIN with recursive allDescendants as ( with recursive ancestorsOf250
        as ( select id, parent from t1 where id = 250 union all select b.id, b.parent
        from ancestorsOf250 as a, t1 as b where a.parent = b.id) traversal order post_order
        select id, parent from ancestorsOf250 union all select b.id, b.parent from
        allDescendants as a, t1 as b where a.id = b.parent) traversal order post_order
        select id, parent from allDescendants
    ref: recursive-cte.yamsql:394
    explain: 'RUNION-DFS POSTORDER q0 { RUNION-DFS POSTORDER q1 { ISCAN(CHILDIDX [EQUALS
        promote(@c19 AS LONG)]) } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1],
        PARENT: KEY:[0]]) | FILTER q1.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT
        AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT) } { RECURSIVE COVERING(PARENTIDX
        <,> -> [ID: KEY:[1], PARENT: KEY:[0]]) | FILTER q0.ID EQUALS _.PARENT | MAP
        (_.ID AS ID, _.PARENT AS PARENT) } | MAP (_.ID AS ID, _.PARENT AS PARENT)'
    task_count: 3135
    task_total_time_ms: 198
    transform_count: 659
    transform_time_ms: 52
    transform_yield_count: 193
    insert_time_ms: 6
    insert_new_count: 447
    insert_reused_count: 15
recursive-cte-tests-with-hints:
-   query: EXPLAIN with recursive c1 as ( select id, parent, 0 as level from t1 use
        index (parentIdx) where id = 250 union all select b.id, b.parent, a.y as level
        from t1 as b use index (parentIdx), (select id, parent, level + 1 as y from
        c1) as a  where a.parent = b.id) traversal order pre_order select id, parent,
        level from c1
    ref: recursive-cte.yamsql:434
    explain: 'RUNION-DFS PREORDER q0 { COVERING(PARENTIDX <,> -> [ID: KEY:[1], PARENT:
        KEY:[0]]) | FILTER _.ID EQUALS promote(@c23 AS LONG) | MAP (_.ID AS ID, _.PARENT
        AS PARENT, @c10 AS LEVEL) } { RECURSIVE COVERING(PARENTIDX <,> -> [ID: KEY:[1],
        PARENT: KEY:[0]]) | FILTER q0.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT
        AS PARENT, q0.LEVEL + @c58 AS LEVEL) } | MAP (_.ID AS ID, _.PARENT AS PARENT,
        _.LEVEL AS LEVEL)'
    task_count: 1068
    task_total_time_ms: 114
    transform_count: 258
    transform_time_ms: 23
    transform_yield_count: 59
    insert_time_ms: 2
    insert_new_count: 132
    insert_reused_count: 4
-   query: EXPLAIN with recursive c1 as ( select id, parent, 0 as level from t1 use
        index (childIdx) where id = 250 union all select b.id, b.parent, a.y as level
        from t1 as b use index (childIdx), (select id, parent, level + 1 as y from
        c1) as a  where a.parent = b.id) traversal order pre_order select id, parent,
        level from c1
    ref: recursive-cte.yamsql:447
    explain: 'RUNION-DFS PREORDER q0 { COVERING(CHILDIDX [EQUALS promote(@c23 AS LONG)]
        -> [ID: KEY:[0], PARENT: KEY:[1]]) | MAP (_.ID AS ID, _.PARENT AS PARENT,
        @c10 AS LEVEL) } { RECURSIVE COVERING(CHILDIDX <,> -> [ID: KEY:[0], PARENT:
        KEY:[1]]) | FILTER q0.PARENT EQUALS _.ID | MAP (_.ID AS ID, _.PARENT AS PARENT,
        q0.LEVEL + @c58 AS LEVEL) } | MAP (_.ID AS ID, _.PARENT AS PARENT, _.LEVEL
        AS LEVEL)'
    task_count: 1037
    task_total_time_ms: 131
    transform_count: 252
    transform_time_ms: 20
    transform_yield_count: 58
    insert_time_ms: 2
    insert_new_count: 127
    insert_reused_count: 3
-   query: EXPLAIN with recursive c1 as ( select id, parent, 0 as level from t1 use
        index (childIdx) where id = 250 and parent is not null union all select b.id,
        b.parent, a.y as level from (select parent, id from t1 use index (childIdx)
        where parent is not null and id is not null) as b, (select id, parent, level
        + 1 as y from c1) as a  where b.id = a.parent) traversal order pre_order select
        id, parent, level from c1
    ref: recursive-cte.yamsql:460
    explain: 'RUNION-DFS PREORDER q0 { COVERING(CHILDIDX [EQUALS promote(@c23 AS LONG),
        [NOT_NULL]] -> [ID: KEY:[0], PARENT: KEY:[1]]) | MAP (_.ID AS ID, _.PARENT
        AS PARENT, @c10 AS LEVEL) } { RECURSIVE COVERING(CHILDIDX [EQUALS q0.PARENT,
        [NOT_NULL]] -> [ID: KEY:[0], PARENT: KEY:[1]]) | FILTER _.ID NOT_NULL | MAP
        (_.ID AS ID, _.PARENT AS PARENT, q0.LEVEL + @c80 AS LEVEL) } | MAP (_.ID AS
        ID, _.PARENT AS PARENT, _.LEVEL AS LEVEL)'
    task_count: 1254
    task_total_time_ms: 144
    transform_count: 300
    transform_time_ms: 22
    transform_yield_count: 69
    insert_time_ms: 2
    insert_new_count: 155
    insert_reused_count: 3
-   query: EXPLAIN with recursive allDescendants as ( select *  from t1 where id =
        50 union all select c.* from allDescendants as p, t1 as c use index (childIdxNoNulls)
        where c.parent is not null and c.parent = p.id) traversal order pre_order
        select * from allDescendants
    ref: recursive-cte.yamsql:473
    explain: 'RUNION-DFS PREORDER q0 { ISCAN(CHILDIDX [EQUALS promote(@c12 AS LONG)])
        } { RECURSIVE COVERING(CHILDIDXNONULLS <,> -> [ID: KEY:[0], PARENT: KEY:[1]])
        | FILTER _.PARENT EQUALS q0.ID AND _.PARENT NOT_NULL | MAP (_.ID AS ID, _.PARENT
        AS PARENT) }'
    task_count: 978
    task_total_time_ms: 89
    transform_count: 247
    transform_time_ms: 19
    transform_yield_count: 72
    insert_time_ms: 1
    insert_new_count: 111
    insert_reused_count: 4
